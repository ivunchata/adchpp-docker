version: '3'

services:
  adchpp:
    image: ivunchata/adchpp:3.0.0
    volumes:
      - ./etc/config:/app/config
      # - ./etc/certs:/app/certs
      # - ./etc/plugins:/app/plugins
      # - ./etc/scripts:/app/bin/scripts
    ports:
      - 2780:2780
